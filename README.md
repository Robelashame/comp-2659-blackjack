Objects and Raster are fine, but in render i couldn't find out how to do path in atari steem emulator so its a bit cluttered,
the render files are render.h render.c bitmap.h bitmap.c testren.c, the make files are for the tests
